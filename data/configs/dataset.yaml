t2i_pretrain:
  dataset_names:
  - t2i
  image_transform_args:
    image_stride: 16
    max_image_size: 256
    min_image_size: 256
  vae_image_transform_args:
    image_stride: 16
    max_image_size: 256
    min_image_size: 256
  is_mandatory: False
  num_used_data: # The sum should be larger that NUM_GPUS x NUM_WORKERS
  - 237387
  weight: 1

t2i_finetune:
  dataset_names:
  - t2i
  image_transform_args:
    image_stride: 16
    max_image_size: 512
    min_image_size: 512
  vae_image_transform_args:
    image_stride: 16
    max_image_size: 512
    min_image_size: 512
  is_mandatory: False
  num_used_data: # The sum should be larger that NUM_GPUS x NUM_WORKERS
  - 237387
  weight: 0

vlm_sft:
  dataset_names:
  - llava_ov
  image_transform_args:
    image_stride: 16
    max_image_size: 384
    min_image_size: 384
    max_pixels: 2_007_040
  frame_sampler_args:
    max_num_frames: 12
    min_num_frames: 8
  is_mandatory: False
  shuffle_lines: False
  shuffle_seed: 0
  num_used_data:
  - 163344
  weight: 0
